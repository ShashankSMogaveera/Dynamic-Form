{
    "steps": [
      {
        "name": "personal_details", 
        "label": "Personal Details",
        "fields": [
          { "name": "fullName", "label": "Full Name", "type": "text", "validations": { "required": true } },
          { "name": "email", "label": "Email", "type": "email", "validations": { "required": true, "pattern": "^[^@\\s]+@[^@\\s]+\\.[^@\\s]+$" } },
          { "name": "phone", "label": "Phone Number", "type": "tel", "validations": { "required": true } }
        ]
      },
      {
        "name": "experience",
        "label": "Work Experience",
        "fields": [
          { "name": "company", "label": "Company Name", "type": "text", "validations": { "required": true } },
          { "name": "position", "label": "Job Position", "type": "text", "validations": { "required": true } },
          { "name": "yearsExperience", "label": "Years of Experience", "type": "number", "validations": { "required": true } }
        ]
      },
      {
        "name": "review_submit",
        "label": "Review & Submit",
        "fields": []
      }
    ]
  }
  